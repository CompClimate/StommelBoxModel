# @package _global_

defaults:
  - override /box_model: basic
  - override /data: autoregressive
  - override /model: mlp
  - override /callbacks: training_path
  - override /trainer: gpu

seed: 42

test: False

plot_bias: True
plot_data: True
plot_gt_pred: True
attr_algorithm: captum.attr.DeepLift
feature_names: null
attr_ylabel: "Shapley Values"

trainer:
  min_epochs: 120
  max_epochs: 120

model:
  optimizer:
    lr: 0.001
    weight_decay: 3e-3

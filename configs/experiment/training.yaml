# @package _global_

defaults:
  - override /box_model: basic
  - override /data: autoregressive
  - override /model: mlp
  - override /callbacks: training_path
  - override /trainer: gpu

seed: 42

trainer:
  min_epochs: 120
  max_epochs: 120

model:
  optimizer:
    lr: 0.001
    weight_decay: 3e-3
